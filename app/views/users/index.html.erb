<header id="content-header">
  <h4><%= pluralize(@users.size, 'User') %></h4>
</header>

<ul id="users">
  <% @users.each do |user| %>
  <li>
    <%= link_to user.email, user %>
  </li>
  <% end %>
</ul>